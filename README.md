# 💱 CurrencyExchange

**CurrencyExchange** — это веб-приложение для **конвертации кастомных валют** с использованием собственных курсов.  
Проект реализован в рамках обучающего роадмапа Сергея Жукова и демонстрирует практическое применение **MVC-подхода, REST API и JDBC** без использования фреймворков.
[Оригинальное ТЗ](https://zhukovsd.github.io/java-backend-learning-course/projects/currency-exchange/)
---

## 🚀 Основные возможности
- Просмотр списка валют
- Добавление новых валют
- Просмотр и редактирование обменных курсов
- Конвертация суммы из одной валюты в другую
- Работа с локальной **SQLite** базой данных
- **REST API**, реализующий CRUD-операции над валютами и курсами
- Деплой на удалённый сервер с помощью **Apache Tomcat**

---

## 🧰 Технологический стек
- **Java 24**
- **Apache Tomcat 7+**
- **JDBC + SQLite**
- **Servlet API (Jakarta/Javax)**
- **Lombok**
- **Jackson**
- **Maven**

---

## 🏗 Архитектура
Приложение построено по принципам **MVC**:
- **Model** – DAO-слой для работы с базой данных (`CurrencyDAO`, `ExchangeRateDAO`)
- **Service** – бизнес-логика (`CurrencyService`, `ExchangeRateService`)
- **Controller** – сервлеты, реализующие REST API
- **View** – фронтенд, взятый от [сюда](https://github.com/zhukovsd/currency-exchange-frontend)

---


## ⚙️ Установка и запуск

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/metty1337/CurrencyExchange.git
```

### 2. Настройте путь к базе данных
Откройте файл:
```
src/main/java/metty1337/currencyexchange/util/DatabaseConnection.java
```
и измените строку:
```java
private static final String DB_URL = "jdbc:sqlite:C:\\Users\\novik\\IdeaProjects\\CurrencyExchange\\data\\db\\sqlite\\database.db";
```
на свой путь к базе данных:
```java
private static final String DB_URL = "jdbc:sqlite:\\your\\path\\to\\database.db";
```

### 3. Соберите проект
```bash
mvn clean package
```

### 4. Разместите артефакт в Tomcat
Скопируйте собранный `.war` из директории `target/`,  
переименуйте его в `ROOT.war` и поместите в папку:
```
/tomcat/webapps/
```

### 5. Запустите Tomcat
Из папки `/tomcat/bin/` выполните:
```bash
startup.bat    # Windows
./startup.sh   # Linux
```

После запуска приложение будет доступно по адресу:
```
http://localhost:8080/
```

### 💡 Минимальные требования:
- JDK 24
- Apache Tomcat 7+

---

## ☁️ Деплой
Проект развёрнут на удалённом сервере и доступен по адресу:  
👉 [http://194.135.46.52:8080/](http://194.135.46.52:8080/)

---

## 🔗 REST API

### 🪙 Валюты
| Метод | Эндпоинт | Описание |
|:------|:----------|:----------|
| `GET` | `/currencies` | Получить список валют |
| `GET` | `/currency/{CODE}` | Получить валюту по коду |
| `POST` | `/currencies` | Добавить новую валюту |

### 💱 Обменные курсы
| Метод | Эндпоинт | Описание |
|:------|:----------|:----------|
| `GET` | `/exchangeRates` | Список всех обменных курсов |
| `GET` | `/exchangeRate/{PAIR}` | Получить курс по паре (например, `USDRUB`) |
| `POST` | `/exchangeRates` | Добавить новый курс |
| `PATCH` | `/exchangeRate/{PAIR}` | Обновить курс |

### 🔄 Конвертация валют
| Метод | Эндпоинт | Пример запроса |
|:------|:----------|:----------------|
| `GET` | `/exchange?from=USD&to=EUR&amount=100` | Конвертирует 100 USD в EUR |

---

## 🧩 Примеры ответов

### ✅ Успешный запрос
```json
{
  "baseCurrency": { "code": "USD", "sign": "$" },
  "targetCurrency": { "code": "EUR", "sign": "€" },
  "rate": 0.92,
  "amount": 100.00,
  "convertedAmount": 92.00
}
```

### ❌ Ошибка
```json
{
  "message": "Валюта не найдена"
}
```

---

## 🌟 Ключевые особенности реализации
- Чистая **MVC-архитектура** без фреймворков
- **Валидация запросов** и обработка ошибок через фильтры
- **JDBC DAO** слой с разделением ответственности
- Поддержка **PATCH** запросов для обновления курсов
- **Логическая обработка конверсии** при отсутствии прямого курса
- Деплой на **удалённый Tomcat сервер**

---

## 🧠 Цели проекта
- Освоение архитектурного паттерна **MVC**
- Реализация **REST API** с корректными HTTP-кодами
- Работа с **JDBC** и **SQLite**
- Подготовка к деплою и развёртыванию Java-приложений
